source "https://rubygems.org"

# Match GitHub Pages’ Jekyll environment locally
# This brings in jekyll and the GitHub Pages-supported plugins
# See: https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Explicitly include the plugin we rely on (also bundled via github-pages)
gem "jekyll-optional-front-matter", group: :jekyll_plugins

# Ruby 3 requires webrick for `jekyll serve`
gem "webrick", "~> 1.8"
